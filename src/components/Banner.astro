---
const { banner } = Astro.locals.starlightRoute.entry.data;
---

<div class="sl-banner" data-pagefind-ignore>
    dylancode.dev has no paywalls, no adverts and no tracking - it is 100% free to use and always will be.
    If you are able to and would like to support my work, please consider a <a href="https://github.com/sponsors/dylanopen">tiny donation</a>.
</div>

<style>
	@layer starlight.core {
		.sl-banner {
			--__sl-banner-text: var(--sl-color-banner-text, var(--sl-color-text-invert));
			padding: var(--sl-nav-pad-y) var(--sl-nav-pad-x);
			background-color: var(--sl-color-banner-bg, var(--sl-color-bg-accent));
			color: var(--__sl-banner-text);
			line-height: var(--sl-line-height-headings);
			text-align: center;
			text-wrap: balance;
			box-shadow: var(--sl-shadow-sm);
			border-bottom: 1px solid var(--sl-color-border);
		}
		.sl-banner :global(a) {
			color: var(--__sl-banner-text);
		}
	}
</style>
