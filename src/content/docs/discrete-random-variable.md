---
title: Discrete random variable
---

As you can probably guess from the two chapters about [random variables](
/random-variable) and [discrete variables](/discrete-variable), a **discrete
random variable** is a discrete variable which changes when we observe it,
because it's also random.

> Discrete random variables are [discrete variables](/discrete-variable) which
> varies based on probabilities and can change when we observe it.

