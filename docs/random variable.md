# Random variable

> A **random variable** is a variable which depends entirely on some level of
> chance. It can change every time it is observed.

## Probability distribution

A random variable will have a *probability distribution* which describes the
likelihood of each possible value of the random variable, or potentially, if
it is [[continuous variable|continuous]], the likelihood of the random variable
taking on a value within a certain [[range]].

## [[flashcards]]

| Question | Answer |
| --- | --- |
| A random variable | A variable which depends entirely on some level of chance and can change every time it is observed. |
| What is a probability distribution? | It describes the likelihood of each possible value of a random variable, or for a continuous variable, the likelihood of it taking a value within a certain range. |

