# Client

A *client* is a computer that makes requests to a [[server]] in order to
get a response.

We can use that response to do things like:
- display a website in a web browser
- download a file from the internet
- send an email
- stream a video
- (really anything else that uses *networking*, e.g. the internet)

The key thing is that:
- the **client** *sends* the request and *receives* the response
- the **[[server]]** *receives* the request and *sends* the response
- (usually)

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is a client in networking? | A computer that makes requests to a server in order to get a response. |
| What can a client do with a response from a server? | Display a website, download a file, send an email, stream a video, or anything else using networking. |
| What role does the client play in a request-response cycle? | The client sends the request and receives the response. |
| What role does the server play in a request-response cycle? | The server receives the request and sends the response. |

