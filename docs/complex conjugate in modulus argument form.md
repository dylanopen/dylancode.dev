# Complex conjugate in modulus-argument form

In [[modulus argument form]], we can find the [[complex conjugate]] of [[complex number]].

> If $z=r(\cos\theta+i\sin\theta)$, then:
> - $z=r(\cos(-\theta)+i\sin(-\theta))$

This is because the conjugate of a [[complex number]] will flip it in the real axis (so that the imaginary number changes sign). This is the same as making the argument negative ('going the other way').

## [[flashcards]]

| Question | Answer |
| --- | --- |
| If $z = r(\cos\theta + i\sin\theta)$, what is the complex conjugate of $z$? | $r(\cos(-\theta) + i\sin(-\theta))$ |
| Why does the conjugate of a complex number result in a negative argument? | The conjugate flips the complex number in the real axis, changing the sign of the imaginary part, which is equivalent to making the argument negative. |

