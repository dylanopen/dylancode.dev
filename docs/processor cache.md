# Processor cache

> Cache is a small amount of very fast memory that's built into the CPU.

Cache is used to store:
- frequently used data and instructions
- data and instructions that the CPU is likely to need soon
- data and instructions that the CPU has recently used
- ...and anything that it doesn't want to have to go to RAM each time to fetch.

## Performance

Cache is much faster than RAM, so if the CPU can get the data it needs from the cache, it can access it much more quickly than if it had to go to RAM to fetch it.

If we have more cache, then the CPU can store more data and instructions in the cache, which means that it can access them more quickly, which means that the overall performance of the CPU will be better. So in general: more cache = better performance.

## [[flashcards]]

| Question | Answer |
| --- | --- |
| Cache | Small amount of very fast memory built into the CPU |
| What types of data and instructions are stored in the cache? | Frequently used data and instructions, data and instructions the CPU is likely to need soon, data and instructions the CPU has recently used, and anything it doesn't want to fetch from RAM each time |
| Why is cache faster than RAM for the CPU to access? | Because cache is much faster than RAM, so the CPU can get data it needs more quickly than going to RAM |
| How does increasing the amount of cache affect CPU performance? | More cache allows the CPU to store more data and instructions in the cache, enabling faster access, which improves overall performance. In general: more cache = better performance. |

