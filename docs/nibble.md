# Nibble

A *nibble* is simply **4 [[bit|bits]]**.

## Number of values represented by nibbles

> A nibble can represent 16 different values.

This is because a nibble is made up of 4 bits, and each [[bit]] can be in one of two
states (0 or 1). Therefore, the total number of combinations of 4 bits is

$$
2^4 = 16
$$

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is a nibble in computing? | A nibble is simply 4 bits. |
| How many different values can a nibble represent? | A nibble can represent 16 different values. |
| Why can a nibble represent 16 different values? | Because a nibble is made up of 4 bits, each with 2 states (0 or 1), so the total number of combinations is $2^4 = 16$. |

