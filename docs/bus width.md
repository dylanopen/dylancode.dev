# Bus width

The width of a [[computer bus|bus]] is the number of bits it can transfer at a time. For example,
if a bus is 64 bits wide, it can transfer 64 bits of data at a time - usually
every [[processor clock]] cycle.

## Performance impact

If the [[address bus]] and [[data bus]] are wider, the [[processor]] can access more memory and transfer more data at a time, which can improve performance. For example, a 64-[[bit]] [[data bus]] can transfer twice as much data as a 32-[[bit]] [[data bus]] in the same amount of time.

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is bus width | The number of bits a bus can transfer at a time. |
| If a bus is 64 bits wide, how much data can it transfer per cycle? | 64 bits of data, usually every processor clock cycle. |
| How does a wider address bus affect performance? | The processor can access more memory. |
| How does a wider data bus affect performance? | The processor can transfer more data at a time, improving performance. |
| Compare a 64-bit data bus to a 32-bit data bus in transfer capacity | The 64-bit data bus can transfer twice as much data as the 32-bit data bus in the same amount of time. |

