# Sector

A *sector* is a part of a circle with:
- two of its edges being the circle's [[radius]]
- its other edge being an [[arc]] of the circle.

Every circle with a sector will have **two sectors**:

## Minor sector

The *smallest* sector of a circle, and it contains the *minor [[arc]]*.

It has an angle of *less than* $\pi$ radians (less than half the circle).

## Major sector

The *largest* sector of a circle, and it contains the *major [[arc]]*.

It has an angle of *more than* $\pi$ radians (more than half the circle).

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is a *sector* of a circle? | A part of a circle with two edges being the circle's [[radius]] and the other edge being an [[arc]]. |
| How many sectors does every circle have? | Every circle has **two sectors**. |
| What is a *minor sector*? | The *smallest* sector of a circle, containing the *minor [[arc]]*, with an angle of *less than* $\pi$ radians (less than half the circle). |
| What is a *major sector*? | The *largest* sector of a circle, containing the *major [[arc]]*, with an angle of *more than* $\pi$ radians (more than half the circle). |

