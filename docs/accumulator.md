# Accumulator

> The accumulator is a [[register]] in the [[processor]] used to store the result of calculations done by the [[arithmetic logic unit]] (ALU). 

When the ALU does any sort of calculation or logic operation, it will store the result of the calculation in the accumulator. The [[control unit]] can then take that result out of the accumulator and use it for whatever it needs to, like storing it in memory, using it in another calculation, etc.


## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is the accumulator in a processor? | A register in the processor used to store the result of calculations done by the arithmetic logic unit (ALU). |
| Where does the ALU store the result of its calculations or logic operations? | In the accumulator. |
| What can the control unit do with the result stored in the accumulator? | Take the result out and use it for whatever it needs, like storing it in memory or using it in another calculation. |

