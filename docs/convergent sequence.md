# Convergent sequence

In a *convergent* [[sequence]], the values will eventually reach a **limit** ($L$).

For (infinitely) large values of $n$:

> $u_{n+1}=u_n=L$

Where:
- $u_n$ is a term in the sequence
- $u_{n+1} is the term in the sequence **after** $u_n$
- $L$ is the limit (the 'endpoint' of the sequence)

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is meant by a *convergent* sequence? | The values will eventually reach a **limit** ($L$). |
| For a convergent sequence, what is the relationship between $u_{n+1}$ and $u_n$ for large $n$? | $u_{n+1}=u_n=L$ |
| In a convergent sequence, what does the variable $L$ represent? | The limit (the 'endpoint' of the sequence) |
| In the notation $u_{n+1}=u_n=L$, what does $u_{n+1}$ represent? | The term in the sequence **after** $u_n$ |

