# Control bus

> The *control bus* is a [[computer bus|bus]] that carries control signals
> between the [[processor]] and the other components of the computer, in order to
> control which components are active at what times, and to tell the components
> which one should be listening to the data.

It basically tells the different components of the computer what to do and when
to do it, so that they can work together to run the program.

## What does the control bus transfer?

- Signals to control the flow of data between the [[processor]] and memory, like
  signals to indicate when the [[processor]] is reading from or writing to memory
  (called 'read' and 'write' signals).
- Signals to control the flow of data between the [[processor]] and other
  components,
  like signals to indicate when the [[processor]] is sending data to or receiving
  data from other components (called 'input' and 'output' signals).
- Signals to control the flow of data between different components, like signals
  to indicate when one component is sending data to another component, so the
  receiving component knows to listen for the data (called 'enable' signals).

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is the control bus? | A bus that carries control signals between the processor and other computer components to control which components are active and when, and to tell components which one should listen to the data. |
| What is the purpose of the control bus? | It tells different components of the computer what to do and when to do it so they can work together to run the program. |
| What is a 'read' signal transferred by the control bus? | A signal to control the flow of data between the processor and memory, indicating when the processor is reading from memory. |
| What is a 'write' signal transferred by the control bus? | A signal to control the flow of data between the processor and memory, indicating when the processor is writing to memory. |
| What are 'input' and 'output' signals on the control bus? | Signals to control data flow between the processor and other components, indicating when the processor is sending data (output) or receiving data (input). |
| What is an 'enable' signal on the control bus? | A signal to control data flow between different components, indicating when one component is sending data so the receiving component knows to listen. |

