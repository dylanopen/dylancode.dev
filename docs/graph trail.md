# Graph trail

A **trail** is just like a [[graph walk|walk]] in a [[graph]], except that
you are not allowed to traverse the same [[graph edge|edge]] more than once.

## Visiting vertices and edges

- You can visit the same [[graph vertex|vertex]] multiple times in a trail.
- However, you cannot traverse the same [[graph edge|edge]] more than once.
