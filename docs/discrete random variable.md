# Discrete random variable

As you can probably guess from the two chapters about [[
/random variable|random variables]] and [[discrete variable|discrete variables]], a **discrete
random variable** is a discrete variable which changes when we observe it,
because it's also random.

> Discrete random variables are [[discrete variable|discrete variables]] which
> varies based on probabilities and can change when we observe it.
