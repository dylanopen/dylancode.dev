# Interpreter

- Interpreters work by reading the [[source code]] **line-by-line** and executing
  it directly.
- This means that the [[source code]] needs to be present every time the program is
  run.

#### Positives of interpreters

- Easier to debug
- More portable (can run on any system with the interpreter)
- No compilation time

#### Negatives of interpreters

- Slower execution
- [[source code]] must be distributed (may expose proprietary code)
- More runtime errors (harder to catch in development)
- Requires interpreter to be installed:
  - May not be available on all systems
  - User needs to install the interpreter themselves
- Less optimization for specific [[hardware]]

## [[flashcards]]

| Question | Answer |
| --- | --- |
| Interpreter (definition) | A program that reads and executes [[source code]] line-by-line, requiring the source code to be present at runtime. |
| What is required for an interpreter to run a program each time? | The [[source code]] must be present every time the program is run. |
| Positive of interpreters - Debugging | Easier to debug. |
| Positive of interpreters - Portability | More portable (can run on any system with the interpreter). |
| Positive of interpreters - Compilation time | No compilation time. |
| Negative of interpreters - Execution speed | Slower execution than compiled code. |
| Negative of interpreters - Source code distribution | [[source code]] must be distributed, which may expose proprietary code. |
| Negative of interpreters - Runtime errors | More runtime errors, making them harder to catch in development. |
| Negative of interpreters - Dependency | Requires interpreter to be installed; it may not be available on all systems and the user needs to install it themselves. |
| Negative of interpreters - Hardware optimization | Less optimization for specific [[hardware]]. |

