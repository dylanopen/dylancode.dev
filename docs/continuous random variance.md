# Continuous random variance

When we calculate the [[variance]] of a [[continuous random variable]], it's exactly the same as [[discrete random variance|calculating the variance]] of a [[discrete random variable]].

> $$
> Var(X)=E(X^2)-E(X)^2
> $$

To find $E(X^2)$, see [[continuous random square expectation]].

## [[flashcards]]

| Question | Answer |
| --- | --- |
| Var(X) = E(X^2) - E(X)^2 | The formula for calculating the variance of a continuous random variable, which is the same as the formula for a discrete random variable. |
| How do you find E(X^2) when calculating variance for a continuous random variable? | See [[continuous random square expectation]]. |

