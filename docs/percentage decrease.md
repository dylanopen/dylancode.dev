# Percentage decrease

If we are asked to decrease a quantity by a certain percentage, we can follow 
these steps:
- Find the *decimal multiplier* of the percentage decrease.
- Multiply the original quantity by the decimal multiplier.

## Finding the decimal multiplier

The decimal multiplier for a percentage decrease can be found using the formula:

$$\text{decimal multiplier} = (\frac{\text{percentage decrease}}{100} - 1)$$

In other words, we [[converting percentages to decimals|convert the percentage decrease to a decimal]],
and then subtract that decimal from 1.

## Finding the percentage decrease

Once we've found the decimal multiplier, simply multiply the original quantity
by the decimal multiplier to find the decreased amount.

### Example: decrease $50 by 20%

1. Find the decimal multiplier:
  - $\text{decimal multiplier} = (\frac{20}{100} - 1) = 0.2 - 1 = 0.8$
2. Multiply the original quantity by the decimal multiplier:
  - $50 \times 0.8 = 40$

Answer: $40$

### Example: decrease 120kg by 15%

1. Find the decimal multiplier:
  - $\text{decimal multiplier} = (\frac{15}{100} - 1) = 0.15 - 1 = 0.85$
2. Multiply the original quantity by the decimal multiplier:
  - $120 \times 0.85 = 102$

Answer: 102kg

### Example: decrease 250 by 5%

1. Find the decimal multiplier:
  - $\text{decimal multiplier} = (\frac{5}{100} - 1) = 0.05 - 1 = 0.95$
2. Multiply the original quantity by the decimal multiplier:
  - $250 \times 0.95 = 237.5$

Answer: 237.5

### Example: decrease $80 by 12.5%

1. Find the decimal multiplier:
  - $\text{decimal multiplier} = (\frac{12.5}{100} - 1) = 0.125 - 1 = 0.875$
2. Multiply the original quantity by the decimal multiplier:
  - $80 \times 0.875 = 70$

Answer: $70$

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What is the formula for finding the decimal multiplier for a percentage decrease? | $\text{decimal multiplier} = (\frac{\text{percentage decrease}}{100} - 1)$ |
| How do you calculate the decimal multiplier from a percentage decrease in words? | Convert the percentage decrease to a decimal, then subtract that decimal from 1. |
| What is the general process to decrease a quantity by a certain percentage? | 1. Find the decimal multiplier of the percentage decrease.<br/>2. Multiply the original quantity by the decimal multiplier. |
| What is the decimal multiplier for a 20% decrease? | $0.8$ (since $\frac{20}{100} - 1 = 0.2 - 1 = 0.8$) |
| What is the result of decreasing \$50 by 20%? | \$40 (since $50 \times 0.8 = 40$) |
| What is the decimal multiplier for a 15% decrease? | $0.85$ (since $\frac{15}{100} - 1 = 0.15 - 1 = 0.85$) |
| What is the result of decreasing 120kg by 15%? | 102kg (since $120 \times 0.85 = 102$) |
| What is the decimal multiplier for a 5% decrease? | $0.95$ (since $\frac{5}{100} - 1 = 0.05 - 1 = 0.95$) |
| What is the result of decreasing 250 by 5%? | 237.5 (since $250 \times 0.95 = 237.5$) |
| What is the decimal multiplier for a 12.5% decrease? | $0.875$ (since $\frac{12.5}{100} - 1 = 0.125 - 1 = 0.875$) |
| What is the result of decreasing \$80 by 12.5%? | \$70 (since $80 \times 0.875 = 70$) |

