# Binomial hypothesis test

If we would like to test [[observed value]]s against a [[binomial distribution]], we can do a [[hypothesis test]].

## Steps

1. Write down what the probability $p$ represents.
2. Write down our [[null hypothesis]], $H_0$ (see [[null hypothesis notation]])
3. Write down our [[alternative hypothesis]], $H_1$ (see [[alternative hypothesis notation]])
4. 

## Template

- Let p = probability that *\[event occurs\]*
- $H_0:p=$ *\[expected probability of event occuring\]*
- $H_1:p$ \[$<$, $>$, $\ne$] 

## Two-tail test

if we have a two-tail test, we should *half* our significance level, because we're splitting it among the lower tail and upper tail.

We then need to find the *expected* number using our sample size and the expected probability (see [[binomial distribution expectation]]).
- If our [[observed value|observed number of occurences]] is greater than the 

## [[flashcards]]

| Question | Answer |
| --- | --- |
| What does a binomial hypothesis test test? | It tests observed values against a binomial distribution. |
| What are the first three steps of a binomial hypothesis test? | 1. Write down what the probability $p$ represents.<br/>2. Write down the null hypothesis $H_0$.<br/>3. Write down the alternative hypothesis $H_1$. |
| In a binomial test, what does $p$ represent? | The probability that *[event occurs]*. |
| What is the notation for the null hypothesis in a binomial test? | $H_0:p=$ *[expected probability of event occuring]* |
| What is the notation for the alternative hypothesis in a binomial test? | $H_1:p$ [ $<$, $>$, $\ne$ ] |
| In a two-tail test, why do we halve the significance level? | Because we are splitting it among the lower tail and upper tail. |
| In a two-tail binomial test, what do we need to find after halving the significance level? | The expected number using our sample size and the expected probability (see binomial distribution expectation). |
| What happens when the observed number of occurrences is greater than the expected number in a two-tail test? | (Implied from context: we check if it falls in the upper tail based on the halved significance level). |

