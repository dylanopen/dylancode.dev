# Digital signal

A digital signal is a signal which can only take on *discrete values* - that
just means that a digital signal can only represent a 0 or a 1.

> Digital signals store **discrete values**.

They're much easier for computers to work with than [[
/[[analogue signal]]|analogue signals]], which is why we convert analogue signals into digital
signals when we want to use them in a computer.

## [[flashcards]]

| Question | Answer |
| --- | --- |
| Digital signal | A signal which can only take on discrete values, representing a 0 or a 1. |
| What kind of values does a digital signal store? | Discrete values. |
| Why are digital signals easier for computers to work with than analogue signals? | They only need to process discrete values (0 or 1) rather than continuous analogue ranges. |
| What happens when we want to use an analogue signal in a computer? | We convert it into a digital signal. |

